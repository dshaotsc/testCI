{
  "description": "Schema for newsletter cancellation data attached to the page view and click event",
  "properties": {
    "newsletter-cancellation-reason": {
      "description": "Why did the user cancel his newsletter subscription",
      "examples": [
        "too-many-newsletters",
        "bad-offers"
      ],
      "type": "string",
      "maxLength": 255
    }
  },
  "additionalProperties": false,
  "type": "object",
  "required": [
  ]
}
